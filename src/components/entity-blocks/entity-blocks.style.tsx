import styled from "styled-components";

export const EntitiesContainer = styled.div`
   display: flex;
   flex-direction: column;
   row-gap: .5rem;
   max-height: 25rem;
   min-height: 4rem;
   overflow: scroll;
`;

export const EnitityContainer = styled.div`
   min-height: 5rem;
   display: flex;
   justify-content: center;
   align-items: center;
   overflow: hidden;
`;